import { FormatVndPipe } from './format-vnd.pipe';

describe('FormatVndPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatVndPipe();
    expect(pipe).toBeTruthy();
  });
});
